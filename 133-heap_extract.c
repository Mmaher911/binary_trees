#include "binary_trees.h"

/**
 * heap_to_sorted_array - converts a Binary Max Heap to a sorted array of integer
 * @root: double pointer to root node of heap
 * Return: value stored in root node
 */

int heap_extract(heap_t **root)
{
	return (root);
}
