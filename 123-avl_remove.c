#include "binary_trees.h"

/**
 * avl_remove - removes node from an AVL tree
 * @root: pointer to root node of
 * the tree for removing node
 * @value: value to remove in tree
 * Return: pointer to new root node of the
 * tree after removing desired value, and after rebalancing
 */

avl_t *avl_remove(avl_t *root, int value)
{
	return (root);
}
